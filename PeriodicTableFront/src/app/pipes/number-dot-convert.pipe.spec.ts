import { NumberDotConvertPipe } from './number-dot-convert.pipe';

describe('NumberDotConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberDotConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
